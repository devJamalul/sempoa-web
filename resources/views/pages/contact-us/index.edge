<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sempoa.id</title>
    @entryPointStyles('app')
</head>

<body class="bg-neutral-100">
    <div class="container mx-auto">
        <a href="{{ route('home') }}">
            <img src="/images/sempoa-logo.svg" class="w-40 mx-auto mt-5" alt="" set="">
        </a>
        <p class="text-center mt-10  mx-auto text-2xl text-darkGreen p-2"> "Bicarakan Kebutuhan Anda dengan Kami"
        </p>


        @if(flashMessages.has('success'))
        <div class="rounded-md bg-green-50 p-4 w-full md:w-1/3 mx-auto">
            <div class="flex">
                <div class="flex-shrink-0">
                    <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                        <path fill-rule="evenodd"
                            d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.857-9.809a.75.75 0 00-1.214-.882l-3.483 4.79-1.88-1.88a.75.75 0 10-1.06 1.061l2.5 2.5a.75.75 0 001.137-.089l4-5.5z"
                            clip-rule="evenodd" />
                    </svg>
                </div>
                <div class="ml-3">
                    <h3 class="text-sm font-medium text-green-800">Pesan Berhasil Terkirim</h3>
                    <div class="mt-2 text-sm text-green-700">
                        <p>Pesan Anda telah berhasil terkirim. Kami akan segera meresponsnya.</p>
                    </div>
                </div>
            </div>
        </div>
        @endif

        {{-- KONTAK US --}}
        <div class="flex flex-col w-full md:w-1/3 shadow-lg rounded-md mx-auto w-full bg-white my-5 hover:shadow-2xl"
            id="contact-us">
            <div class="flex   flex-col text-center my-3 ">
                <div class="px-3 text-darkGreen text-xl font-bold">KONTAK KAMI</div>
            </div>
            <form action="{{ route('contact-us.send') }}" method="post">
                {{ csrfField() }}
                <div class="p-6">
                    <div class="w-full my-3">
                        <label class="block tracking-wide text-gray-700 text-xs font-bold " for="full_name">
                            Nama Lengkap <span class="text-sm text-red-500">*</span>
                        </label>
                        <input placeholder="Nama Lengkap"
                            class="w-full p-1 px-3 rounded-xl mt-1.5 ring-1 {{ flashMessages.has('errors.full_name') ? 'is-invalid' : '' }} ring-black/20 focus:outline-none outline-none focus:ring-lightGreen ring-2 duration-200"
                            type="text" name="full_name" id="full_name">
                        @if (flashMessages.has('errors.full_name'))
                        <div class="message-error">
                            {{ flashMessages.get('errors.full_name') }}
                        </div>
                        @endif
                    </div>
                    <div class="w-full my-3">
                        <label class="block tracking-wide text-gray-700 text-xs font-bold " for="email">
                            Email <span class="text-sm text-red-500">*</span>
                        </label>
                        <input placeholder="Email"
                            class="w-full p-1 px-3 rounded-xl mt-1.5 ring-1 ring-black/20 {{ flashMessages.has('errors.email') ? 'is-invalid' : '' }} focus:outline-none outline-none focus:ring-lightGreen ring-2 duration-200"
                            type="email" name="email" id="email">

                        @if (flashMessages.has('errors.email'))
                        <div class="message-error">
                            {{ flashMessages.get('errors.email') }}
                        </div>
                        @endif
                    </div>

                    <div class="w-full my-3">
                        <label class="block tracking-wide text-gray-700 text-xs font-bold " for="tlpn">
                            No Tlpn <span class="text-sm text-red-500">*</span>
                        </label>
                        <input placeholder="No Tlpn"
                            class="w-full p-1 px-3 rounded-xl mt-1.5 ring-1 {{ flashMessages.has('errors.phone') ? 'is-invalid' : '' }} ring-black/20 focus:outline-none outline-none focus:ring-lightGreen ring-2 duration-200"
                            type="text" name="phone" id="tlpn">
                        @if (flashMessages.has('errors.phone'))
                        <div class="message-error">
                            {{ flashMessages.get('errors.phone') }}
                        </div>
                        @endif
                    </div>

                    <div class="w-full my-3">
                        <label class="block tracking-wide text-gray-700 text-xs font-bold " for="message">
                            Pesan <span class="text-sm text-red-500">*</span>
                        </label>
                        <textarea name="message"
                            class="w-full p-1 px-3 rounded-xl mt-1.5 ring-1 ring-black/20 {{ flashMessages.has('errors.message') ? 'is-invalid' : '' }} focus:outline-none outline-none focus:ring-lightGreen ring-2 duration-200"></textarea>
                        @if (flashMessages.has('errors.message'))
                        <div class="message-error">
                            {{ flashMessages.get('errors.message') }}
                        </div>
                        @endif
                    </div>

                    <div class=" mt-5 flex  flex-col justify-center">
                        <button type="submit"
                            class="bg-darkGreen text-white w-full font-semibold py-2 px-10 rounded   transition-all text-center space-x-2">
                            <span class="text-center">Kirim Sekarang</span></button>
                    </div>
                </div>

            </form>


        </div>
        {{-- END KONTAK US --}}
</body>

</html>